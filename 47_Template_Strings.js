console.clear();
const f_name='Lockie';
const l_name='Ferguson';

function GetFullName(f_name,l_name) {
    return `${f_name} ${l_name}`;
}
const Greetings=`Hello ${GetFullName(f_name,l_name) }`;
console.log(Greetings)

let r=110;
let m=220;
let w=333;
let f=123;
const rmwfEXp=`rgba(${r},${m},${w},${f})`;
console.log(rmwfEXp);